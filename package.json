{
  "scripts": {
    "build": "cd Frontend && npm install && npm run build"
  },
  "dependencies": {
    "react-scripts": "^5.0.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.2",
    "parcel": "^2.16.3",
    "vite": "^7.3.0"
  }
}